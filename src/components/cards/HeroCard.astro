---

---

<div
  class=`card relative w-full md:h-72 lg:h-full rounded-xl lg:overflow-hidden
  before:rounded-[inherit] before:h-full before:content-[''] before:w-full before:absolute before:top-0 before:left-0 before:z-30 
  before:opacity-0 before:transition-opacity before:duration-500 hover:before:opacity-100
  after:rounded-[inherit] after:h-full after:content-[''] after:w-full after:absolute after:top-0 after:left-0 after:z-10 
  after:opacity-0 after:transition-opacity after:duration-500 group-hover/cards:after:opacity-100 group
  bg-radial-gradient`
>
  <div
    class="content p-6 bg-secondary bg-opacity-30 background-image rounded-[inherit] z-20 m-[1px] relative"
  >
    <div class="flex flex-col justify-end items-end font-title text-text gap-3">
      <span class="text-5xl tracking-tighter">Amine Rahmane</span>
      <span class="text-3xl tracking-tight"
        >Développeur et créateur d'identité</span
      >
    </div>
  </div>
</div>

<style lang="scss">
  .bg-radial-gradient::before {
    background: radial-gradient(
      400px circle at var(--mouse-x) var(--mouse-y),
      rgba(228, 100, 71, 0.3),
      transparent 40%
    );
  }

  .bg-radial-gradient::after {
    background: radial-gradient(
      200px circle at var(--mouse-x) var(--mouse-y),
      rgba(228, 100, 71, 0.2),
      transparent 40%
    );
  }

  .card > .content {
    height: calc(100% - 2px);
    width: calc(100% - 2px);
  }

  .background-image {
    background-image: linear-gradient(
        rgba(8, 3, 2, 0.65),
        rgba(8, 3, 2, 0.65)
      ),
      url('../../assets/background-amine.jpg');
  }
</style>
