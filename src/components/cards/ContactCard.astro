---

---

<div
  id="contact"
  class=`card cursor-pointer relative w-full h-56 lg:h-full rounded-xl lg:overflow-hidden
  before:rounded-[inherit] before:h-full before:content-[''] before:w-full before:absolute before:top-0 before:left-0 before:z-30 
  before:opacity-0 before:transition-opacity before:duration-500 hover:before:opacity-100
  after:rounded-[inherit] after:h-full after:content-[''] after:w-full after:absolute after:top-0 after:left-0 after:z-10 
  after:opacity-0 after:transition-opacity after:duration-500 group-hover/cards:after:opacity-100 group/thronocrigger
  bg-radial-gradient col-span-auto row-span-auto`
>
  <div
    class="content relative w-full h-full bg-secondary bg-opacity-30 rounded-[inherit] z-20 m-[1px] bg-cover bg-center
    px-6 py-4
    flex flex-row flex-wrap items-center justify-center
    lg:p-6"
  >
    <div
      class="text font-title prose text-background bg-text px-3 py-2 text-clamp-title w-max tracking-tighter"
    >
      À propos
    </div>
  </div>
</div>

<script>
  import { navigate } from 'astro:transitions/client'

  const handleClick = () => {
    document.getElementById('contact')!.addEventListener('click', (e) => {
      navigate('/contact')
    })
  }

  document.addEventListener('astro:page-load', handleClick)
</script>

<style lang="scss">
  .bg-radial-gradient::before {
    background: radial-gradient(
      400px circle at var(--mouse-x) var(--mouse-y),
      rgba(228, 100, 71, 0.5),
      transparent 40%
    );
  }

  .bg-radial-gradient::after {
    background: radial-gradient(
      200px circle at var(--mouse-x) var(--mouse-y),
      rgba(228, 100, 71, 0.3),
      transparent 40%
    );
  }

  .card {
    background-size: 100% 100%;

    &:hover {
      z-index: 100;
      .content {
        transform: scale(1.02);
        transition: transform 0.2s ease-in-out;
      }
    }
  }

  .dotted-grid {
    z-index: 1;
    background: radial-gradient(rgba(255, 255, 255, 0.05) 9%, transparent 9%);
    background-position: 0% 0%;
    background-size: 8vmin 8vmin;
  }

  .card > .content {
    height: calc(100% - 2px);
    width: calc(100% - 2px);
  }

  .text {
    &::after {
      content: '_';
      @apply text-background;
      display: inline-block;
      animation: blink 1.4s step-end infinite;
    }
  }

  @keyframes blink {
    0% {
      opacity: 1;
    }
    50% {
      opacity: 0;
    }
    100% {
      opacity: 1;
    }
  }
</style>
